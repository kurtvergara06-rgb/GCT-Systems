# Model #3 - Delay Prediction (SAMPLE / DEMONSTRATION prototype)
#
# Dependency subset used by the delay-prediction pipeline. The shared
# python_engine environment already provides these; the versions pinned here
# match the venv that the pipeline is developed/tested against.
# Runs as a library from the root python_engine venv (no separate install needed).

pandas>=2.2
numpy>=1.26
scikit-learn==1.9.0
joblib>=1.3
fastapi>=0.110
pydantic>=2.5
uvicorn>=0.29
httpx>=0.27   # used only by the test client (test_delay.py)