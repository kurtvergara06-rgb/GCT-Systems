<article class="analytics-card ranking-card"><x-analytics.card-header title="Top Routes by Trips" description="Highest-volume corridors"/><div class="ranking-list refined-ranking-list">@forelse(collect($fleet['routes']??[]) as $route)<div class="refined-ranking-row"><span class="refined-rank-number">{{ $loop->iteration }}</span><div class="refined-ranking-main"><div class="refined-ranking-title-row"><strong>{{ $route->label }}</strong><span>{{ number_format($route->trips) }} trips</span></div><div class="metric-bar refined-metric-bar"><span style="width:{{ $route->progress }}%"></span></div><div class="refined-ranking-meta"><span><i class="fa-regular fa-clock"></i>{{ number_format($route->average_duration,1) }} min avg.</span><span><i class="fa-solid fa-chart-pie"></i>{{ number_format($route->share,1) }}% of trips</span></div></div></div>@empty<p class="ranking-empty">No route records match the current filter.</p>@endforelse</div></article>