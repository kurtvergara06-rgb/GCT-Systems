<article class="analytics-card ranking-card"><x-analytics.card-header title="Busiest Buses" description="Highest recorded trip activity"/><div class="ranking-list refined-ranking-list">@forelse(collect($fleet['busActivity']??[]) as $bus)<div class="refined-ranking-row"><span class="refined-rank-number">{{ $loop->iteration }}</span><div class="refined-ranking-main"><div class="refined-ranking-title-row"><strong>{{ $bus->bus }}</strong><span>{{ number_format($bus->trips) }} trips</span></div><div class="metric-bar refined-metric-bar"><span style="width:{{ min(100,max(4,$bus->share*6)) }}%"></span></div><div class="refined-ranking-meta"><span><i class="fa-solid fa-road"></i>{{ number_format($bus->distance,1) }} km</span><span><i class="fa-solid fa-chart-pie"></i>{{ number_format($bus->share,1) }}% trip share</span></div></div></div>@empty<p class="ranking-empty">No bus activity matches the current filter.</p>@endforelse</div></article>